<%= form_with(model: user) do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
        <% user.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field input-group mb-3">
    <%# <%= form.label :first_name %>
    <%= form.text_field :first_name, class: "form-control", placeholder:"Nombre" %>
  </div>

  <div class="field input-group mb-3">
    <%= form.text_field :last_name, class: "form-control", placeholder:"Apellido" %>
  </div>

  <div class="field input-group mb-3">
    <%= form.text_field :github, class: "form-control" , placeholder:"Cuenta de github"%>
  </div>

  <div class="field input-group mb-3">
    <%= form.text_field :wsp, class: "form-control", placeholder:"Teléfono whatsapp" %>
  </div>

  <div class="field input-group mb-3">
    <%= form.number_field :userlog_id,
      class: "form-control",
      value: current_userlog.id,
      id: :user_userlog_id,
      type: :hidden
    %>
  </div>
  
  <div class="actions">
    <%= form.submit class:"btn btn-success" %>
  </div>
<% end %>
